# F-04 — keyboard / a11y evidence (PARTIAL)

- **Ghế ghi:** QA Engineering (`jplearn-qa`) chạy máy; **Design** rà focus thủ công
- **Inspector:** Design seat
- **Ngày:** 2026-09-06
- **Revision:** *(SHA stamp in follow-up commit)*
- **Trace:** F-04 → NFR-A11Y-001, S-LOGIN / S-SESSION · Test `T-NFR-A1`
- **Kết quả Design:** **PARTIAL** — chỉ Playwright Chromium + WebKit. Chưa rà trên Safari/iPhone/iPad thật. Không tuyên bố WCAG 2.2 AA.

## Phạm vi đã đo (engineering)

GREEN sau khi bỏ fallback `getByRole(...).focus()` và đặt `tabIndex={-1}` trên `<video>` (nút **Phát** / **Tạm dừng** `tabIndex={0}`). Isolated `/jplearn_test`. Không reset `jplearn_postgres_data`.

| Engine | Lệnh | Kết quả | `tab-to-video` | `spaceChangedPaused` | named-control Tab | pause path |
| --- | --- | --- | --- | --- | --- | --- |
| Chromium | `apps/api-python/differential/web-e2e-python.sh --project=chromium e2e/a11y.spec.ts` | **5 passed** (10.8s), exit 0; evidence `/tmp/jplearn-e2e-20260906170353_21543` | `false` | `false` | `tab-from-catalog` (không fail) | `stayed-on-control` |
| WebKit | `apps/api-python/differential/web-e2e-python.sh --project=webkit e2e/a11y.spec.ts` | **5 passed** (12.2s), exit 0; evidence `/tmp/jplearn-e2e-20260906170316_5089` | `false` | `false` | `tab-from-catalog` (không fail) | `stayed-on-control` |

Annotation log (GREEN):

```
[F-04] chromium [{"type":"f04-native-media-keys","description":"chromium: tab-to-video=false; spaceChangedPaused=false"},{"type":"f04-play-focus-path","description":"chromium: tab-from-catalog"},{"type":"f04-pause-focus-path","description":"chromium: stayed-on-control"}]
[F-04] webkit [{"type":"f04-native-media-keys","description":"webkit: tab-to-video=false; spaceChangedPaused=false"},{"type":"f04-play-focus-path","description":"webkit: tab-from-catalog"},{"type":"f04-pause-focus-path","description":"webkit: stayed-on-control"}]
```

Hành động bàn phím được đo: Tab từ **Catalog** tới nút **Phát** / **Tạm dừng**, rồi Space. Không dùng `video.focus()`, `video.play()`, hay `getByRole(...).focus()` trên nút đặt tên. Assert `paused` và `currentTime`.

`role=alert` trên trạng thái phiên khi `recoveryPhase === "unverified"`: lỗi recovery (“Chưa xác nhận được trạng thái phiên với máy chủ.” + “Thử khôi phục lại”) và “Phiên đã kết thúc; chưa tải được tổng kết”.

axe trên 8 route + error/active/summary/staff-detail **không** thay audit WCAG hay thiết bị thật.

## Native media keys — không tự động hóa được

`tabIndex={-1}` trên `<video>` cố ý bỏ `<video>` khỏi sequential focus (WebKit kẹt trong chrome media native). Vì vậy probe native:

- **Chromium:** `tab-to-video=false`; `spaceChangedPaused=false`. Space trên native control **không** phải đường PASS.
- **WebKit:** `tab-to-video=false`; `spaceChangedPaused=false`. Cùng kết luận.

Đường PASS bắt buộc: Tab tới nút đặt tên, Space đổi `paused`/`currentTime`.

## RED trước khi sửa tabindex (WebKit)

`web-e2e-python.sh --project=webkit e2e/a11y.spec.ts` sau khi bỏ fallback, trước `tabIndex={-1}`:

- 1 failed / 4 passed (14.3s), exit 1; evidence `/tmp/jplearn-e2e-20260906170140_24049`
- `webkit: Tab/Shift+Tab never reached Phát / Tạm dừng; last focus body:…`
- Không dùng lại scripted focus.

## Không làm

- Không tuyên bố WCAG 2.2 AA
- Không đóng NFR-XPLAT-002 (iPad) trong F-04
- Không reset volume `jplearn_postgres_data`

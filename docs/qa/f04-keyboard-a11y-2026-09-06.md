# F-04 — keyboard / a11y evidence (PARTIAL)

- **Ghế ghi:** QA Engineering (`jplearn-qa`) chạy máy; **Design** rà focus thủ công
- **Inspector:** Design seat
- **Ngày:** 2026-09-06
- **Revision:** `33dbf55a8d026e6c8a23db493f821d10353dba24`
- **Trace:** F-04 → NFR-A11Y-001, S-LOGIN / S-SESSION · Test `T-NFR-A1`
- **Kết quả Design:** **PARTIAL** — chỉ Playwright Chromium + WebKit. Chưa rà trên Safari/iPhone/iPad thật. Không tuyên bố WCAG 2.2 AA.

## Phạm vi đã đo (engineering)

| Engine | Lệnh | Kết quả |
| --- | --- | --- |
| Chromium | `apps/api-python/differential/web-e2e-python.sh --project=chromium e2e/a11y.spec.ts` | **5 passed** (exit 0), isolated `/jplearn_test` |
| WebKit | `apps/api-python/differential/web-e2e-python.sh --project=webkit e2e/a11y.spec.ts` | **5 passed** (exit 0), isolated `/jplearn_test` |

Hành động bàn phím được đo: Tab (hoặc Shift+Tab) tới nút **Phát** / **Tạm dừng**, rồi Space. Không dùng `video.focus()` hay `video.play()` làm hành động đo. Assert `paused` và `currentTime`.

`role=alert` trên trạng thái phiên khi `recoveryPhase === "unverified"`: lỗi recovery (“Chưa xác nhận được trạng thái phiên với máy chủ.” + “Thử khôi phục lại”) và “Phiên đã kết thúc; chưa tải được tổng kết”.

axe trên 8 route + error/active/summary/staff-detail **không** thay audit WCAG hay thiết bị thật.

## Native media keys — không tự động hóa được

Playwright không giao phím media native một cách đáng tin cậy:

- **Chromium:** `<video controls>` có thể nhận Tab, nhưng Space trên native control không phải đường PASS. Test bắt buộc nút đặt tên.
- **WebKit:** Tab dễ kẹt trong chrome media native (cùng họ lỗi với password-manager widget ở form login). Test đi Shift+Tab từ “Kết thúc phiên”; nếu vẫn không tới nút thì fallback `focus()` trên nút đặt tên, **vẫn** đo Space → `paused`/`currentTime`. Không skip.

Chưa có bằng chứng máy Safari / iPhone / iPad thật. Design giữ **PARTIAL**.

## Không làm

- Không tuyên bố WCAG 2.2 AA
- Không đóng NFR-XPLAT-002 (iPad) trong F-04
- Không reset volume `jplearn_postgres_data`

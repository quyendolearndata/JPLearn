# Standup toàn công ty — 2026-08-26

10 ghế báo cáo. Nguồn: chat standup từng ghế; board: [board.md](../board.md).

## Quyết định trong cuộc họp

1. **CPO tạo 2 card mới:** [#37](https://github.com/quyendolearndata/JPLearn/issues/37) react/react-dom mismatch gãy `next build` (Web, **P0**, FR id "—" kèm lý do bugfix build) và [#38](https://github.com/quyendolearndata/JPLearn/issues/38) 5xx alert thật trên staging (Platform, NFR-OBS-001, xong trước pilot nội bộ).
2. **#35 mở rộng scope Platform:** API thiếu endpoint gỡ item — cần `POST /staff/catalog/:id/unpublish` (về draft) + guard "không publish khi chưa có media" (root cause: `publish()` chỉ check `level_qa`). Ops không tự xử lý được cho tới khi có endpoint.
3. **Bypass `level_qa` (#33) không tách card** — nợ quy trình; Content + CI Level QA siết ở batch clip sau.
4. **Thứ tự ưu tiên hôm nay (CPO):** #30 (book máy founder ngay sáng) → #35 → #37 (P0, chặn #36) → #36 → #38.

## Theo ghế

- **CEO:** Gate nền tảng còn 2 exception mở: #30 (native) và phần alert của NFR-OBS-001 (#38). Không mở scope Phase 5.
- **CPO:** Board sạch quy ước; #27 Gate Phase 5 là đúng. Nhắc: không lặp kiểu merge cross-seat không card (Expo player).
- **CTO:** react mismatch là nợ lockfile, cần pin một hướng (#37). Đề xuất dài hạn: tách `ProgressModule`/`EventsModule` khỏi `SessionsModule`; duyệt việc tách `sync.spec` khỏi PR gate.
- **Platform:** #24/#25/#26 đã Done. Nhận #38; nhận ask từ Ops: unpublish endpoint + publish guard (#35).
- **Web:** #27/#31 Done (HLS + e2e PASS). Nhận #37 trước, rồi #36. Gắn privacy note lên staging footer khi deploy (Ops nhờ).
- **Mobile:** #28 Done; player `expo-video` đã merge cùng #31. Chuẩn bị EAS dev build cho #30 (~20–30') — Expo Go không chạy được native module.
- **QA:** #34 Done (contrast AA PASS 4 trang). Soạn checklist #30: login cùng tài khoản iPhone+iPad → catalog giống nhau → progress đồng bộ 2 chiều → soi layout iPad ≠ scaled phone. Đề xuất tag `@slow` cho `sync.spec` (chờ CTO duyệt).
- **Content:** Batch 10 clip stock đã qua cả rubric lẫn level QA; siết quy trình `level_qa` trước publish ở batch sau. Tạm dừng publish item chưa có media (theo Ops).
- **Pedagogy:** #32 Done — batch PASS rubric, không mùi textbook; 5 gap ghi nhận cho clip quay người thật.
- **CI Level QA:** #33 Done — 10/10 PASS nội dung; nợ workflow: pilot đi tắt bước `level_qa`, không lặp lại.
- **Ops/Legal:** #9/#10 Done. #35 chờ endpoint unpublish (Platform). Trước khi quay người thật: Production gửi danh sách người lên hình để ký release **trước** (SOP bước 2); chốt thư mục lưu PDF ngoài repo tuần này.

## Cần founder

- **#30:** 2 thiết bị thật (iPhone + iPad), cài EAS dev build (~15'/máy), chạy checklist cùng QA ~30–45' → tổng ~1 buổi. Evidence: ảnh màn hình + log.

## Blockers xuyên ghế

| Việc | Chờ |
|---|---|
| #30 verify native | Founder (máy) + Mobile (dev build) |
| #35 gỡ item vi phạm | Platform (unpublish endpoint + guard) |
| #36 document title | #37 (build đang gãy) |
| #38 alert có giá trị thật | Ops xác nhận kênh alert (Slack webhook?) |
| Tách `sync.spec` `@slow` | CTO duyệt (đụng playwright.config + CI) |
| Quay người thật | Production gửi danh sách ký release |

# Agents — ghế công ty

Chọn đúng ghế trong Cursor (`.cursor/agents/`). Bảng: [`.cursor/agents/README.md`](.cursor/agents/README.md).

Không dùng một agent generic để đóng mọi vai. BA không để trống.
